// ui.js — UI panel management for Clever
export function showPanel(name) {
	// Optional: Show side panels, analysis views, etc.
	console.log(`[Clever] Showing panel: ${name}`);
}

export function hidePanel(name) {
	// Optional: Hide side panels
	console.log(`[Clever] Hiding panel: ${name}`);
}
